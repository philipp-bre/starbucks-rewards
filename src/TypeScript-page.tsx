
import Button from './TypeScript-test'

export default function Home() {
    return (
        <main className="min-h-screen flex justify-center items-center">
            <Button type="submit" autoFocus={true} / >
        </main>
    );
};